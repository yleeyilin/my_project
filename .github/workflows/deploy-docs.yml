name: Deploy Sphinx Documentation to GitHub Pages

on:
  push:
    branches:
      - main 
  repository_dispatch:
    types: [create-pull-request]

permissions:
  contents: write
  pull-requests: write
  
jobs:
  createPullRequest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x  

      - name: Install dependencies to generate docs
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate Sphinx Documentation
        run: |
          python generate_docs.py
      
      - name: Create auto-documentation branch
        run: |
          git checkout main
          git pull origin main
          git branch -D auto-documentation || true
          git checkout -b auto-documentation
  
      - name: Add Generated Documentation
        run: |
          git add docs/build 
        
      - name: Commit Changes
        run: |
          git diff --quiet || git commit -m "Add sphinx documentation"
          
      - name: Pull remote changes
        run: |
          git pull origin auto-documentation || echo "No changes to pull"

      - name: Push Branch
        run: |
          git push origin auto-documentation

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Add sphinx documentation
          body: >
            This PR is auto-generated by 
            [create-pull-request](https://github.com/peter-evans/create-pull-request).
          title: 'Merge Sphinx Documentation'
          labels: automated pr
          branch: auto-documentation
          base: main
